spring.mail: #todo was für brevo amk
  host: ${SMTP_HOST}
  port: 587
  username: ${SMTP_USERNAME}
  password: ${SMTP_PASSWORD}
  properties.mail.smtp:
    auth: true
    starttls.enable: true
    starttls.required: true